// Get results from retrieval service (array of answers)
// Make request to openai service to compile them into markdown
// Return markdown string

export const AggregationService = {
  aggregate: async (countryCode: string) => {
    // Find assistant
    // FOR EACH QUESTION
    // Ask assistant for reply
    // Add to results list
    // END OF FOR EACH
    // Summarize in markdown
    // Return markdown string
  },
};

export default AggregationService;
